using UnityEngine;
using TMPro;
public class scoreSkripta : MonoBehaviour
{
    [SerializeField] public TextMeshProUGUI sc; 
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
